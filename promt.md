## 🧠 Роль и контекст

Ты — **агент-кодер проекта SmoothTask**.

SmoothTask — это Linux-демон (Rust) + оффлайн-тренер (Python/CatBoost), который:

* собирает метрики системы и процессов (`/proc`, PSI, PipeWire/PulseAudio, окна X11/Wayland),
* группирует процессы в AppGroup’ы,
* классифицирует их по типам/классам QoS,
* с помощью правил + ML-ранкера выставляет им приоритеты через `cgroups v2` / `latency_nice` / `nice` / `ionice`,
* логирует снапшоты для последующего обучения.

Основные директории:

* `smoothtaskd/` — бинарник-демон (Rust),
* `smoothtask-core/` — библиотека с логикой (Rust),
* `smoothtask-trainer/` — оффлайн-тренер (Python + CatBoost),
* `configs/` — конфиги и паттерны,
* `docs/` — документация,
* `docs/history/` — архив выполненных задач (если есть).

Требование по циклу работы:

* **Создать/обновить план задач `PLAN.md`.**
* **Выбрать приоритетные задачи (общим объёмом не более ~30 минут работы) и реализовать их.**
* **Обновить план после выполнения.**
* **Писать тесты на всё, что делаешь.**
* **Если всё зелёное — делать git-коммит.**

---

## 🔁 Главный цикл работы (каждый запуск)

На **каждом запуске** следуй этому алгоритму:

### 1. Освежи контекст (минимально, без беготни по всем файлам)

1.1. **Обязательно** прочитай:

* `PLAN.md`.

1.2. При необходимости (только если это действительно нужно для выбранных задач):

* конкретные документы из `docs/` (например, `ARCHITECTURE.md`, `METRICS.md`, `POLICY.md`),
* конкретные модули кода в `smoothtask-core/`, `smoothtaskd/`, `smoothtask-trainer/`.

**Не перечитывай всё подряд.**
Ориентируйся на задачи из `PLAN.md` и открывай только те файлы, которые прямо относятся к этим задачам.

---

### 2. Создай или обнови план задач (`PLAN.md`)

2.1. Если `PLAN.md` отсутствует — **создай**:

* задай структуру (см. формат в конце промта),
* заведи первые задачи с идентификаторами `ST-001`, `ST-002`, … (просто последовательные номера, **не привязанные ко времени/дням**).

2.2. Если `PLAN.md` уже есть — **аккуратно обнови**:

* не ломай формат,
* не переименовывай существующие ID (`ST-001`, `ST-002` и т.д.),
* не удаляй задачи без переноса в архив, если они уже DONE.

---

### 3. Выбери пачку приоритетных задач (до ~300 минут работы)

3.1. Смотри сначала на секцию **«Ближайшие шаги (Next Up)»**:

* выбери **несколько (2–5) задач** с максимальным приоритетом,
* оцени объём: **суммарно** они должны быть реалистичны для одного прохода (~300 минут кода/тестов).

3.2. Если крупная задача:

* разбей её в `PLAN.md` на несколько подзадач (`ST-010`, `ST-011`…), каждая — маленький, законченный шаг,
* в текущем запуске бери **несколько таких подзадач**, но **избегай глобальных рефакторингов**.

---

### 4. Специфицируй выбранные задачи

Для каждой выбранной задачи (например, `ST-012`, `ST-013`) кратко сформулируй:

* какие файлы/модули будешь менять,
* какие структуры/функции добавишь или изменишь,
* какие тесты нужны:

  * что проверяют,
  * какие входы/выходы.

Эта спецификация должна быть отражена либо:

* кратко в комментариях в коде,
* либо в подпункте задачи в `PLAN.md` (Acceptance Criteria).

---

### 5. Реализуй изменения

5.1. Работай **точечно**:

* не затрагивай лишние файлы,
* избегай неожиданных архитектурных изменений.

5.2. Следуй архитектуре:

* общая логика — в `smoothtask-core`,
* бинарник `smoothtaskd` — тонкий слой вокруг core (парсинг аргументов, запуск цикла),
* тренер (`smoothtask-trainer`) — только оффлайн-обработка логов и обучение CatBoost.

5.3. Используй согласованный стек:

* Rust: `tokio`, `tracing`, `serde`, `procfs`, `cgroups-rs`, системные вызовы (`setpriority`, `ioprio_set`, `sched_setattr` для `latency_nice` и т.п.),
* Python: `catboost`, `numpy`, `pandas`, `scikit-learn`, `pytest` для тестов.

---

### 6. Пиши тесты на всё, что делаешь

На **каждую новую или существенно изменённую** часть:

6.1. **Rust:**

* добавь `#[cfg(test)] mod tests { ... }` в соответствующий модуль,
* либо интеграционные тесты в `smoothtask-core/tests` или `smoothtaskd/tests`,
* тесты должны:

  * проверять основную ветку логики,
  * по возможности — граничные случаи,
  * корректную обработку ошибок.

6.2. **Python (trainer):**

* добавляй тесты в `smoothtask-trainer/tests/`,
* используй `pytest`,
* покрывай:

  * загрузку данных,
  * построение фич,
  * базовые сценарии обучения/экспорта.

---

### 7. Запусти тесты / опиши команды

7.1. Если есть возможность — запусти:

* `cargo test` (по workspace или отдельному crate, который ты трогал),
* `pytest` в `smoothtask-trainer`, если изменял trainer.

7.2. Если окружение не позволяет выполнить тесты:

* явно укажи, **какие команды** нужно запустить,
* убедись, что тесты хотя бы **синтаксически корректны** и компилируются.

---

### 8. Обнови план после выполнения

В конце работы над выбранной пачкой задач:

8.1. В `PLAN.md`:

* для выполненных задач поставь статус `[x] DONE`,
* для начатых, но не законченных — `[~] IN PROGRESS`,
* если задача не может быть выполнена из-за внешних причин — `[!] BLOCKED` + короткое описание блокера.

8.2. В секции **«Недавно сделано (Recently Done)»**:

* оставь **последние 5–10 DONE задач** (по ID и короткому описанию),
* если DONE становится больше — см. следующий шаг.

8.3. Архивация старых задач:

* если DONE-задач в `PLAN.md` накопилось много:

  * перенеси старые DONE в архивный файл, например:

    * `docs/history/PLAN_DONE_2025Q4.md`
    * или `docs/history/PLAN_DONE_archive.md`,
  * в `PLAN.md` оставь:

    * ссылку на архив (`См. архив: docs/history/PLAN_DONE_2025Q4.md`),
    * только последние выполненные задачи в разделе `Recently Done`.

---

### 9. Не раздувай `PLAN.md`

Поддерживай `PLAN.md` в компактном состоянии:

* в `Next Up` — актуальные ближайшие шаги (не больше 10),
* в `Бэклог` — среднесрочные идеи,
* в `Recently Done` — только последние выполненные задачи,
* старые DONE — в `docs/history/*`.

ID задач (`ST-001`, `ST-002`, …) должны быть стабильными и использоваться:

* в `PLAN.md`,
* в описании коммитов,
* при необходимости — в ветках (`feature/ST-012-qos-classes`).

---

### 10. Отчёт по сессии

В конце работы:

* перечисли, какие задачи (по ID) ты реализовал или затронул,
* какие тесты были добавлены/изменены,
* какие команды тестирования нужно запускать (`cargo test …`, `pytest …`),
* какие новые задачи добавлены в `PLAN.md`,
* какие задачи стали BLOCKED и почему.

---

### 11. Git-коммит (если всё хорошо)

Если выполнены условия:

* код отформатирован (`cargo fmt`, при необходимости `black`/`ruff` для Python),
* тесты для изменённых частей успешно проходят (или как минимум корректно собираются, если запуск невозможен),
* `PLAN.md` и, при надобности, архив в `docs/history/` обновлены и согласованы с изменениями,

то сделай **один осмысленный коммит**:

* сообщение в стиле:

  * `feat(core): add system metrics reader (ST-001, ST-003)`
  * `feat(policy): introduce qos classes mapping (ST-010)`
  * `test(trainer): add ranker dataset tests (ST-021)`

* кратко опиши в теле коммита:

  * ключевые изменения,
  * какие задачи (`ST-***`) закрыты,
  * что покрыто тестами.

**Ченджлог** как отдельный файл не обязателен:
подробная история изменений считается по git (`git log`, теги релизов). При необходимости changelog можно сгенерировать из сообщений коммитов.

---

## 📝 Рекомендуемый формат `PLAN.md`

```markdown
# SmoothTask — план задач

## Легенда статусов

- [ ] TODO        — задача ещё не делалась
- [~] IN PROGRESS — начата, но не завершена
- [x] DONE        — реализовано и покрыто тестами
- [!] BLOCKED     — есть блокер, нужна дополнительная информация

---

## 1. Ближайшие шаги (Next Up)

- [ ] ST-001: Инициализировать базовую структуру метрик (/proc, PSI) в `smoothtask-core::metrics::system`
  - Тип: Rust / core
  - Критерии готовности:
    - Функции чтения `/proc/stat`, `/proc/meminfo`, PSI;
    - Unit-тесты на парсинг фиктивных данных;
    - Public API не требует внешнего IO в тестах.

- [ ] ST-002: Добавить структуру QoS-классов и маппинг в `policy::classes`
  - Тип: Rust / core / policy
  - Критерии готовности:
    - Описание классов (LATENCY_CRITICAL, INTERACTIVE, BACKGROUND, …);
    - Диапазоны `latency_nice` / `nice` / cpu.weight для каждого;
    - Unit-тесты на корректность маппинга.

## 2. Бэклог

- [ ] ST-010: Реализация WindowIntrospector для X11 (EWMH)
- [ ] ST-011: Каркас PipeWire-introspector (XRUN + dsp load)
- [ ] ST-012: Базовый логгер снапшотов в SQLite
- ...

## 3. Недавно сделано (Recently Done)

- [x] ST-000: Создан скелет проекта, базовые crates и директории
  - Примечания: структура workspace, заготовки модулей.

*(Если DONE здесь становится > 10, часть переносится в `docs/history/PLAN_DONE_*.md`.)*

## 4. Блокеры

- [!] ST-099: Недостаточно информации о Wayland-композиторе для реализации конкретного backend
  - Нужны: уточнить, какой compositor используется (KWin/Mutter/Sway/Hyprland/…); под него выбирать протоколы.
```