# SmoothTask — план задач

## Легенда статусов

- [ ] TODO      — задача ещё не делалась
- [~] IN PROGRESS — начата, но не завершена
- [x] DONE      — реализовано и покрыто тестами
- [!] BLOCKED   — есть блокер, нужна дополнительная информация

---

## 1. Ближайшие шаги (Next Up)

- [ ] ST-010: Собрать базовые метрики CPU/memory/loadavg из `/proc`
  - Тип: Rust / core / metrics
  - Критерии готовности: функции парсинга `/proc/stat`, `/proc/meminfo`, `/proc/loadavg` с юнит-тестами.
- [ ] ST-020: Экспорт снапшотов в SQLite и чтение в трейнере
  - Тип: Rust/Python / storage/trainer
  - Критерии готовности: запись снапшотов в БД, утилита чтения в smoothtask-trainer, базовые тесты.
- [ ] ST-030: Вернуть зависимость onnxruntime, когда появится стабильная версия с нужными фичами
  - Тип: Rust / модель
  - Критерии готовности: обновлённая зависимость, проверка совместимости сборки.

## 2. Бэклог

- [x] ST-001: Добавить парсинг PSI и снимок глобальных метрик в `smoothtask-core::metrics::system`
  - Итог: добавлены структуры PSI, чтение `/proc/pressure` с настройкой пути и юнит-тесты на парсинг и ошибки.

## 3. Готово (Done)

- [x] ST-000: Добавить валидацию конфигурации и покрыть пример YAML тестами
  - Примечания: добавлена валидация Config::load, проверки путей и тест загрузки.

## 4. Блокеры

- [!] ST-099: Нет информации по Wayland-композитору для WindowIntrospector
  - Нужны: уточнить целевой API и доступные источники метрик.
